import { PagedQueryResult } from './paged-query-result.model';

describe('PagedQueryResult', () => {
  it('should create an instance', () => {
    expect(new PagedQueryResult()).toBeTruthy();
  });
});
